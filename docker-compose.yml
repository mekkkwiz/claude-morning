services:
  claude-morning:
    build: .
    volumes:
      - ./data:/root/.claude
      - .:/workspace
    command: daemon
    environment:
      - CLAUDE_MORNING_CRON_SCHEDULE=0 8-23/5 * * *
      - TZ=Asia/Bangkok
    restart: unless-stopped
